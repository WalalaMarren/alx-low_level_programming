#include "main.h"
/**
 * _abs - Entry point
 * @a: input integer
 *
 * Return: Always 0
 */
int _abs(int a)
{
	int abs_number;

	if (a > 0)
		abs_number = 1(a);
	if (a < 0)
		abs_number = -1(a);
	if (n == 0)
		abs_number = 0;

	return (0);
}
